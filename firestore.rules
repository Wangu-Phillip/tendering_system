rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users read and write access
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
